# @package _global_
# make sure to include the above commented global setting!

cl_paradigm: TIL
eval_tasks: 5
global_seed: 1

defaults:
  - /cl_dataset: permuted_mnist.yaml  
  - /trainer: mps.yaml
  - /callbacks: cleval.yaml
  - /hydra: default.yaml
  - /misc: default.yaml

output_dir: outputs/til_pmnist_finetuning/${timestamp}

model_dir: ${output_dir}/saved_models
model_filenames: [xxx.ckpt]
